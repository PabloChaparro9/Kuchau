const formulario = document.getElementById("buscador")
const producto = document.querySelector('#carta-1')
const carta = document.querySelector('.carta')

console.dir(carta)
console.dir(formulario)
console.log(producto)
/* 
const filtrar = () =>{
    const buscar = formulario.value.toLowerCase();
    for(let producto of productos){
        let nombre = producto.innerText.toLowerCase();
        if(nombre.indexOf(buscar) !== -1){
            console.log(productos[producto])
        }
    }
}
formulario.addEventListener('keyup', filtrar) */